#Overrides when running on dev server

env: 'development'
minify: false
sourcemaps: true
log: debug

site:
  url: "http://localhost:3000/"
  analytics: "" #don't count hits from localhost in analytics
